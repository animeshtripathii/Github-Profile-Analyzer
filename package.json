{
  "name": "github-profile-analyzer",
  "version": "1.0.0",
  "description": "A backend service that fetches public GitHub user data, extracts meaningful insights, and persists them in a MySQL database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "npm --prefix client install && npm --prefix client run build"

  },
  "keywords": [
    "github",
    "profile",
    "analyzer",
    "api",
    "mysql",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.6",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "express-validator": "^7.2.1",
    "mysql2": "^3.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
